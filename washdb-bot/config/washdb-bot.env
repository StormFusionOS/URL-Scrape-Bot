# WashDB-Bot Environment Configuration for systemd services
# Copy to /etc/washdb-bot/washdb-bot.env and customize paths for your system
#
# Usage:
#   sudo mkdir -p /etc/washdb-bot
#   sudo cp config/washdb-bot.env /etc/washdb-bot/
#   sudo vim /etc/washdb-bot/washdb-bot.env  # Edit paths as needed
#
# Then install services:
#   ./scripts/install_systemd_services.sh

# Installation paths
WASHDB_HOME=/home/rivercityscrape/URL-Scrape-Bot/washdb-bot
WASHDB_VENV=${WASHDB_HOME}/venv
WASHDB_LOGS=${WASHDB_HOME}/logs

# Service user (change if running as different user)
WASHDB_USER=rivercityscrape

# Database connection (from your .env file)
# DATABASE_URL=postgresql://washbot:password@localhost:5432/washbot_db
