DATABASE_URL=postgresql+psycopg://washbot:change_me_strong@localhost:5432/washdb
LOG_LEVEL=INFO

# Yellow Pages Configuration
YP_BASE=https://www.yellowpages.com/search

# Bing Discovery Configuration
# BING_API_KEY=your_bing_web_search_api_key_here        # Optional: Bing Web Search API v7 key (leave blank for HTML scraping mode)
BING_MODE=auto                                            # Mode: 'api' (requires key), 'html' (scraping), 'auto' (uses API if key present)
BING_CRAWL_DELAY_SECONDS=3.0                              # Rate limiting delay between requests (seconds)
BING_PAGES_PER_PAIR=5                                     # Default number of pages to crawl per category/state pair

# General Crawl Settings
CRAWL_DELAY_SECONDS=2
MAX_CONCURRENT_SITE_SCRAPES=5
